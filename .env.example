# Copy to .env for local development with `netlify dev`.
# In production these are set in the Netlify dashboard, never committed.

# The shared password that gates the whole app.
APP_PASSWORD=change-me

# Used to HMAC-sign session cookies. Generate with: openssl rand -hex 32
SESSION_SECRET=change-me-to-a-long-random-string
